 #ifndef DEBUG_FRAME
#define DEBUG_FRAME

//#include <common/environment/debug_world_helper.hpp>

#include <common/debug.hpp>

#include <chrono>
#include <iostream>
#include <memory>
#include <vector>

//#include <common/space/object3d.hpp>

//class Object3dExpanded;

//class SpatialObjectExpanded;

class DebugFrame
{
public:
    DebugFrame(){};
    
//    template <class T>
//    void registerObject(std::shared_ptr<Object3d<T> > * children) {
        //get the coords
        //get the dimensions
//        std::cout << children << std::endl;
        
//        std::shared_ptr<SpatialObjectExpanded> * pd = static_cast< std::shared_ptr<SpatialObjectExpanded> * >(children);
        
//        std::vector<std::shared_ptr<Object3d> > chs;
        
//        Object3dExpanded u_poi;
         
//        u_poi = static_cast<Object3dExpanded> (children->handle);
        
//        this->trackedElements.push_back(pd);
//    }
    
private:
//    std::vector<std::shared_ptr<SpatialObjectExpanded> * > trackedElements;
};

#endif